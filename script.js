(function() {
    $(document).ready(function() {
        var repo    = $('h1').text().split('/');
        var project = repo[0].toUpperCase();
        var name    = repo[1];
        $('td').each(function() {
            var self = $(this);
        });
    });
})();
